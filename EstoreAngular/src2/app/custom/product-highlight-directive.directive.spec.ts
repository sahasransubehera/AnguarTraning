import { ProductHighlightDirectiveDirective } from './product-highlight-directive.directive';

describe('ProductHighlightDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ProductHighlightDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
